<UserControl x:Class="OximyWindows.Views.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="420" d:DesignWidth="340">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <StackPanel Grid.Row="0" VerticalAlignment="Center" Margin="24,20,24,0">

            <!-- Status Icon (80x80 circle like Mac) -->
            <Grid Width="80" Height="80" HorizontalAlignment="Center" Margin="0,0,0,16">
                <Ellipse x:Name="StatusCircle"
                         Width="80" Height="80"
                         Opacity="0.15"/>
                <TextBlock x:Name="StatusIcon"
                           Text="&#xEA18;"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="36"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Grid>

            <!-- Status Text -->
            <TextBlock x:Name="StatusText"
                       Text="Monitoring Active"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,4"/>

            <!-- Port Info (shown when connected) -->
            <TextBlock x:Name="PortText"
                       Text="Port 1030"
                       FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"
                       Visibility="Collapsed"/>

            <!-- Toggle Button -->
            <Button x:Name="CaptureToggleButton"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Padding="16,10"
                    Margin="32,0,32,8"
                    HorizontalAlignment="Stretch"
                    Click="CaptureToggleButton_Click">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="CaptureButtonIcon"
                               Text="&#xE768;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBlock x:Name="CaptureButtonText"
                               Text="Start Monitoring"
                               FontSize="14"
                               FontWeight="SemiBold"/>
                </StackPanel>
            </Button>

            <!-- Certificate Warning -->
            <TextBlock x:Name="CertWarningText"
                       Text="Install certificate in Settings first"
                       FontSize="12"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"
                       Visibility="Collapsed"/>

            <!-- Error Message (if any) -->
            <Border x:Name="ErrorPanel"
                    Background="#FEF2F2"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,16,0,0"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="&#xE783;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               Foreground="{DynamicResource ErrorBrush}"
                               VerticalAlignment="Center"/>

                    <TextBlock x:Name="ErrorText"
                               Grid.Column="1"
                               Text="Error message"
                               FontSize="12"
                               Foreground="{DynamicResource ErrorBrush}"
                               TextWrapping="Wrap"
                               Margin="8,0"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="2"
                            Content="Retry"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Padding="8,4"
                            FontSize="11"
                            Click="RetryButton_Click"/>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Bottom Info Card -->
        <Border Grid.Row="1"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="10"
                Margin="16,0,16,16"
                Padding="16,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Device Row -->
                <Grid Grid.Row="0">
                    <TextBlock Text="Device"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="DeviceText"
                               Text="Device Name"
                               FontSize="12"
                               FontWeight="Medium"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"/>
                </Grid>

                <!-- Separator -->
                <Rectangle Grid.Row="1"
                           Height="1"
                           Fill="{DynamicResource BorderBrush}"
                           Margin="0,10"/>

                <!-- Organization Row -->
                <Grid Grid.Row="2">
                    <TextBlock Text="Organization"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock x:Name="WorkspaceText"
                                   Text="My Workspace"
                                   FontSize="12"
                                   FontWeight="Medium"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center"/>
                        <Button Style="{DynamicResource IconButtonStyle}"
                                Padding="4"
                                Margin="8,0,0,0"
                                Click="SettingsButton_Click"
                                ToolTip="Settings">
                            <TextBlock Text="&#xE76C;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
