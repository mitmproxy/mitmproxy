const defaultState = {
    list: [],
    isFetching: false,
    updateBeforeFetch: [],
    byId: {},
    indexOf: {},
    views: {}
}

export default function reducer(state = defaultState, action) {
    switch (action.type) {
        default:
            return state
    }
}
