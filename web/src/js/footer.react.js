/** @jsx React.DOM */

var Footer = React.createClass({
    render : function(){
        var style = {
            textAlign: "center"
        };
        return (<footer>
            <span className="label label-success">transparent mode</span>
            </footer>);
    }
});