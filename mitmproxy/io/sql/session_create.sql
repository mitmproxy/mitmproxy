CREATE TABLE FLOW (
FID INTEGER PRIMARY KEY,
MID INTEGER,
BID INTEGER,
CONTENT BLOB
);

CREATE TABLE META (
MID INTEGER PRIMARY KEY,
INTERCEPTED INTEGER,
MARKED INTEGER,
MODE VARCHAR(20)
);

CREATE TABLE BODY (
BID INTEGER,
BREQ BLOB,
BRES BLOB
);

CREATE TABLE ANNOTATION (
AID INTEGER PRIMARY KEY,
FID INTEGER,
TYPE VARCHAR(20),
CONTENT BLOB
);